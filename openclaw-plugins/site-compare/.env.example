OPENCLAW_SITE_COMPARE_SECRET=replace-me
# Required: forward compare payloads to the real Vibepress compare worker.
# This worker already opens WordPress + React pages and returns the metrics
# payload expected by ai-pipeline. The plugin no longer falls back to mock data.
SITE_COMPARE_FORWARD_URL=http://127.0.0.1:5000/api/site/compare
